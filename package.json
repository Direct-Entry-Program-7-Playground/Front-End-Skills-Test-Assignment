{
  "name": "font-end-skills-test-1",
  "version": "1.0.0",
  "dependencies": {
    "@fortawesome/fontawesome-free": "^5.15.1",
    "bootstrap": "^4.5.3",
    "jquery": "^3.6.0"
  },
  "scripts": {
    "watch": "tsc --watch",
    "dev": "parcel --out-dir ./docs serve index.html",
    "clean": "rm -rf ./docs ./dist",
    "build": "rm -rf ./docs ./dist && parcel build index.html --out-dir ./docs --public-url ./"
  },
  "devDependencies": {
    "@types/jquery": "^3.5.6",
    "parcel-bundler": "^1.12.5",
    "typescript": "^4.4.3"
  }
}
